{
  "name": "Eagler",
  "build": {
    "dockerfile": "Dockerfile"  // Make sure Dockerfile path is correct
  },
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/${localWorkspaceFolderBasename},type=bind",  // Mount the local workspace
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}"
}
